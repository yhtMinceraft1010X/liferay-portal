definition {

	@summary = "Add an item to the end of the list."
	macro add {
		Variables.assertDefined(parameterList = "${item}");

		if ("${list}" == "") {
			var newList = "${item}";
		}

		else {
			var newList = '''${list},${item}''';
		}

		return "${newList}";
	}

}