definition {

	macro resizeImage {
		Select(
			locator1 = "Select#IMAGE_RATIO",
			value1 = "${dimension}");
	}

	macro rotateImage {
		Click(
			key_text = "reload",
			locator1 = "Icon#ANY");
	}

	macro saveChanges {
		Button.clickSave();

		SelectFrameTop();
	}

}