@default = "uploadFile"
definition {

	function uploadFile {
		WaitForSPARefresh();

		selenium.waitForElementPresent();

		selenium.mouseOver();

		selenium.uploadFile();

		selenium.assertJavaScriptErrors();

		selenium.assertLiferayErrors();
	}

}